<br>
<% @posts.each do |x| %>
  <div class="white">
  <%= image_tag x.image.url(:medium) %>
  <br>
  <%= x.description %><br>
  <i>Posted <%= distance_of_time_in_words(x.created_at, Time.now) %> ago</i>
  <br><br>
<% end %>